Dockerizated model from regression task<br>

To run use docker volume:<br>
docker run -v \<path to input file folder>:\<container path to input file folder> name_image \<path in container to input file> 
